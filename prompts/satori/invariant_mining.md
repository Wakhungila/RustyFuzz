Mine candidate invariants from the supplied function and protocol context.

Return strict JSON. Invariants should be testable locally and should reference concrete observed state, balances, shares, debt, reserves, roles, or oracle values.
