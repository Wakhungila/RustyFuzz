Convert the supplied hypothesis into a RustyFuzzJobSpec.

Return strict JSON. The job must be local/fork-safe, must not broadcast live transactions, and must mark missing ABI/address/fork data explicitly.
